openapi: "3.0.0"
info:
  version: "0"
  title: "boats-utils-test"
  description: A sample API
  contact:
    name: acrontum
    email: support@acrontum.de
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: /v1

paths:
  /asdf:
    get:
      summary: summary
      description: description
      responses:
        '200':
          description: Success
          content:
            text/plain:
              schema:
                type: string

components:
  schemas:
    $ref: components/schemas/index.yml
